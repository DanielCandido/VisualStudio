@model WebMVC.Models.PessoaViewModel
@{
    ViewData["Title"] = "Error";
}
<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model.Items)
        {
            <tr>
                <td>@item.id</td>
                <td>@item.nome</td>
            </tr>
        }
    </tbody>
</table>